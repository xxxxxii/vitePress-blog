import { RendererRichOptions, TwoslashRenderer, TransformerTwoslashOptions } from 'shikiji-twoslash';
export { defaultHoverInfoProcessor } from 'shikiji-twoslash';
import { ShikijiTransformer } from 'shikiji';
import { VitePressPluginTwoslashOptions as VitePressPluginTwoslashOptions$1 } from 'vitepress-plugin-twoslash';

declare function rendererFloatingVue(options?: VitePressPluginTwoslashOptions$1 & RendererRichOptions): TwoslashRenderer;

interface VitePressPluginTwoslashOptions extends TransformerTwoslashOptions, RendererRichOptions {
    /**
     * Requires adding `twoslash` to the code block explicitly to run twoslash
     * @default true
     */
    explicitTrigger?: boolean;
}
/**
 * Create a Shikiji transformer for VitePress to enable twoslash integration
 *
 * Add this to `markdown.codeTransformers` in `.vitepress/config.ts`
 */
declare function transformerTwoslash(options?: VitePressPluginTwoslashOptions): ShikijiTransformer;

export { type VitePressPluginTwoslashOptions, rendererFloatingVue, transformerTwoslash };
